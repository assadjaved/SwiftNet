platform :ios, '16.0'

target 'SwiftNetApp' do
  use_frameworks! :linkage => :static

  # Pods for SwiftNetApp
  pod 'SwiftNet', :path => '.', :inhibit_warnings => false

  target 'SwiftNetAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftNetAppUITests' do
    # Pods for testing
  end

end
